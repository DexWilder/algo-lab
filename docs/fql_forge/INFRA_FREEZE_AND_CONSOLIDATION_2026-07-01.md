# Infra Freeze & Consolidation (2026-07-01)

> **The organization layer is declared complete.** No new infrastructure unless it directly: prevents a known recurrence,
> unlocks a richer data tier, supports a specific queued strategy family, removes stale/decorative infra, or deepens a real
> SCREEN_PASS candidate. Everything else is EDGE WORK. Enforced by guardrail (git backlog / unrun-harness) + self-audit.

## Functional infra (KEEP — the machine)
causality_audit · deflated_sharpe · forge_trial_ledger (layered) · forge_system_guardrails (12 checks) · capture_inbound +
triage · validate_strategy_expression · validate_data_file · forge_novelty_engine (generative, learning-weighted) ·
forge_family_map (computed, tiered) · forge_candidate_ladder (enforced) · adversarial_result_review (data-tier aware) ·
forge_dashboard · forge_self_audit (watchdog + JSON artifact) · update_learning_state (loop closure) · term_structure ·
**forge_microstructure_1m (T3 harness — the frontier)**.

## Stale / one-off / decorative (ARCHIVE-or-IGNORE candidates)
- ~200 `forge_cycle_*` / one-off sprint scripts from the primitive-sweep era (trial-N shows 1679 primitive trials). The
  guardrail unrun-harness check already flags built-but-never-run scripts. **Policy:** do not resurrect; mine the ledger for
  their verdicts instead of re-running. Physical archive deferred (low value, non-blocking) — they don't run automatically.
- 138 forge docs: many are point-in-time checkpoints. **Policy:** canonical state lives in the dashboard + family map +
  learning_state + inbound; docs are history, not truth.

## New scoreboard (candidates, not tools)
rich-data (T3+) packets tested · candidates deepened · source/novelty packets generated · **data assets activated** ·
DSR/search-N credible candidates · guardrail + self-audit clean status. **NOT:** script count, doc count.

## Freeze status
ACTIVE as of 2026-07-01. New infra this cycle was loop-closure + data-tier gate + T3 harness — all fall under "unlock richer
tier / prevent recurrence / close learning loop," i.e. permitted. Next cycle target: **edges, not tooling.**
